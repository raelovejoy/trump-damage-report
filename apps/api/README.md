# API

Read-only endpoints for the frontend.

## Endpoints
- `GET /health`
- `GET /incidents?date=YYYY-MM-DD`
- `GET /graph?date=YYYY-MM-DD`

## Env
- `DATABASE_URL` (Postgres connection string)
- `PORT` (default 8080)
