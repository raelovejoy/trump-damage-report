# Web Static (Fastmail)

This folder is hosted as a static website (HTML/CSS/JS).

## Configuration
Set the API base URL inside `app.js`:
- `const API_BASE = "https://<your-api-domain>"`

## Hosting
Upload these files to the Fastmail website folder for your domain.
